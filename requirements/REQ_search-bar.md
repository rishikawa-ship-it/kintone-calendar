# 要件定義書: カレンダー検索欄機能

**文書番号**: REQ_search-bar
**作成日**: 2026-05-20
**最終更新日**: 2026-05-20（v2: ハイライト/フォーカス遷移仕様を追加、キーボードショートカットを削除）
**作成者**: designer (サブエージェント)
**ステータス**: レビュー中（§11 未確定事項あり — ユーザー判断待ち）
**関連文書**: REQ_edit-permission-extension.md, plugin/src/js/desktop.js, plugin/src/css/desktop.css

---

## 改版履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| v1 | 2026-05-20 | 初版作成（検索欄基本機能） |
| v2 | 2026-05-20 | ハイライト/フォーカス遷移仕様を追加。キーボードショートカット（`/` キー）を削除（ユーザー判断で不採用確定）。マッチ候補 activeIndex 管理・Enter/上下キーナビゲーション仕様を追加 |

---

## 目次

1. [概要・背景・目的](#1-概要背景目的)
2. [用語](#2-用語)
3. [機能要件](#3-機能要件)
4. [非機能要件](#4-非機能要件)
5. [UI 仕様](#5-ui-仕様)
6. [データ構造](#6-データ構造)
7. [既存実装への変更点](#7-既存実装への変更点)
8. [影響範囲 / 非干渉確認](#8-影響範囲--非干渉確認)
9. [受入基準](#9-受入基準)
10. [検証項目](#10-検証項目)
11. [未確定事項・リスク・前提](#11-未確定事項リスク前提)

---

## 1. 概要・背景・目的

### 1.1 背景

KC Calendar プラグインは現状、カレンダー上に表示される予定を「全て / 自分のみ / 他人のみ」のフィルタドロップダウン（`KC.FilterDropdown`）で絞り込む機能を持つ。しかしこのフィルタはユーザー属性ベースであり、「キーワードで特定の予定を素早く見つける」用途には対応していない。

予定数が増加するにつれ、目的の予定を目視で探すコストが増大する。検索欄を追加することで、タイトル等のキーワードを入力するだけで目的の予定を即座に絞り込めるようにする。

### 1.2 目的

1. カレンダーヘッダーバーに検索入力欄を追加し、ユーザーがキーワードで予定を絞り込めるようにする
2. 絞り込み結果をカレンダー上に即座に反映する（リアルタイム検索）
3. マッチした予定をハイライト・フォーカスインジケータで明示し、Enter/上下キーで候補間を移動できるようにする
4. 既存のフィルタ機能・権限色機能との非干渉を保証する

### 1.3 スコープ

- **Phase 1（本要件）**: タイトルフィールドのみを対象としたクライアント側フィルタ。リアルタイム検索。マッチ候補のハイライト・フォーカス遷移。
- **Phase 2（将来）**: 検索対象フィールドの拡張、プラグイン設定での対象フィールド選択、kintone API クエリ投影。

---

## 2. 用語

| 用語 | 定義 |
|---|---|
| 検索クエリ | ユーザーが検索欄に入力したテキスト文字列 |
| 検索フィルタ | 検索クエリによる絞り込み処理。既存の eventFilter（mine/others/all）とは独立した第2のフィルタ層 |
| クライアント側フィルタ | kintone REST API への追加リクエストを発生させず、取得済みの `KC.State.events` をメモリ内で絞り込む方式 |
| 一致予定（マッチ予定） | 検索クエリにマッチする予定。カレンダー上にハイライト表示される |
| 非一致予定 | 検索クエリにマッチしない予定。半透明表示（opacity: 0.25）にする |
| アクティブ候補 | マッチ候補の中で現在フォーカス中の予定。フォーカスインジケータ（太枠）で示す |
| activeIndex | マッチ候補リスト内でアクティブ候補が何番目かを示す 0 始まりのインデックス |
| マッチ候補リスト | 現在のクエリにマッチする予定を表示順に並べた配列。フォーカス遷移の巡回に使用する |
| デバウンス | 連続入力時に最後の入力から一定時間待ってから処理を実行する仕組み |
| IME 確定 | 日本語入力時に変換キー（Enter / スペース）で文字を確定させる操作。`compositionend` イベントが発火する |
| KcEvent | desktop.js 内でレコードから変換した予定オブジェクト。`title` / `start` / `end` 等を持つ |

---

## 3. 機能要件

### FR-1: 検索欄の配置

カレンダーヘッダーの `.kc-head-right` 内に検索欄（`id="kc-search"` のラッパー要素）を追加する。

- 配置位置: FilterDropdown（`#kc-filter`）の左側
- ヘッダー右側の要素順序（左→右）:
  - 検索欄（新規）
  - フィルタドロップダウン（既存）
  - ビュードロップダウン（既存）
  - 全画面ボタン（既存）
  - ⚙設定ボタン（既存）

### FR-2: 検索欄の UI 構成

検索欄は以下の 3 要素で構成する。

1. 検索アイコン（`🔍` または SVG、ボタンではなく装飾）
2. テキスト入力欄（`<input type="search" id="kc-search-input">`）
   - `placeholder`: 「予定を検索」
   - `aria-label`: 「予定を検索」
3. クリアボタン（`<button id="kc-search-clear">` / `×`）
   - 検索クエリが空のときは非表示
   - 検索クエリが 1 文字以上あるときに表示

フォーカスはユーザーが検索欄をクリックして当てる。キーボードショートカットによるフォーカス移動は実装しない（v2 確定）。

### FR-3: 検索対象フィールド（Phase 1）

Phase 1 では `fieldTitle` フィールド（`KC.Config.FIELD.title`）のみを検索対象とする。

- `KcEvent.title` に対して部分一致・大文字小文字区別なし（`toLowerCase()` 正規化）で検索する
- 他フィールド（メモ・場所・メールアドレス・アカウント名 等）への拡張は Phase 2 で検討する

### FR-4: 検索方式

- **部分一致**: クエリがタイトルの任意の位置に含まれれば一致と判定する
- **大文字小文字区別なし**: `query.toLowerCase()` と `title.toLowerCase()` を比較する
- **スペース区切り AND 検索**: 検索クエリをスペース（全角・半角）で分割し、全トークンが全て含まれる予定のみ一致と判定する
  - 例: 「会議 月次」と入力 → タイトルに「会議」かつ「月次」が含まれる予定が一致
  - トークンが 1 つのみの場合は通常の部分一致と同等

### FR-5: リアルタイム検索とデバウンス

- `input` イベントを監視し、入力が変化するたびにフィルタを適用してカレンダーを再描画する
- デバウンス: 最後の `input` イベントから **300ms** 後にフィルタ処理を実行する
- IME 入力中（`compositionstart` 〜 `compositionend` の間）はデバウンスタイマーを起動しない。`compositionend` 発火後に 300ms デバウンスを開始する

### FR-6: 非一致予定の表示

検索クエリが非空のとき、非一致予定は**半透明（opacity: 0.25）**にする。

- 一致予定: 通常表示（opacity 変更なし）+ マッチハイライト（後述 FR-11 参照）
- 非一致予定: `opacity: 0.25` のインラインスタイルを付与
- 検索クエリが空: 全予定を通常表示（opacity スタイルをリセット、ハイライト解除）

### FR-7: 検索状態のビュー間共有

検索クエリはビュー切替（月 / 週 / 日）をまたいで維持する。

- ユーザーが月ビューで「会議」と入力し、週ビューに切り替えた場合も「会議」での絞り込みが継続される
- 再描画時に `KC.SearchFilter.query` の値を参照してフィルタを適用する
- ビュー切替時は `activeIndex` を -1（未選択）にリセットする

### FR-8: 結果 0 件時のメッセージ

検索クエリが非空かつ一致する予定が 0 件の場合、カレンダーグリッド内（または直下）に「「{query}」に一致する予定はありません」というメッセージを表示する。

- メッセージ要素: `id="kc-search-empty"` の `<div>`
- 表示条件: 検索クエリ非空 かつ フィルタ適用後の表示件数が 0
- 非表示条件: 検索クエリが空 または 1 件以上表示される

### FR-9: 検索クエリの初期化

- ページロード時: 検索クエリは空（`''`）で初期化する
- sessionStorage への保存はしない（§11.5 参照）
- kintone ビュー再表示（`app.record.index.show` イベント再発火）時: 検索クエリをリセットし、入力欄を空にする

### FR-10: 既存 EventFilter との連携

検索フィルタと既存の `KC.EventFilter`（mine / others / all）は**独立した 2 層のフィルタ**として機能する。

- 適用順序: `KC.EventFilter.apply()` → `KC.SearchFilter.apply()`
- いずれのフィルタも通過した予定のみカレンダーに描画される
- 一方のフィルタを変更しても他方のフィルタはリセットされない

### FR-11: マッチ候補のハイライト表示（v2 追加）

検索クエリが非空のとき、一致予定（マッチ予定）は視覚的にハイライトして区別する。

- 一致予定の DOM 要素に `.kc-event--match` クラスを付与する
- アクティブ候補（`activeIndex` で指定された予定）には `.kc-event--match-active` クラスを追加付与する
- `.kc-event--match` の表示例: 通常マッチ = 細枠（`outline: 2px solid #2563eb`）
- `.kc-event--match-active` の表示例: アクティブ候補 = 太枠（`outline: 3px solid #1d4ed8`）+ 背景色強調（`box-shadow: 0 0 0 3px rgba(37,99,235,0.3)`）
- 実際のスタイル値は実装時に調整可。上記は参考値

### FR-12: マッチ候補へのフォーカス遷移（v2 追加）

検索欄フォーカス中に、キー操作でマッチ候補間を移動する。

#### FR-12-1: 検索入力後の初期フォーカス

- IME 確定後（`compositionend` 発火後）またはデバウンス 300ms 経過後にフィルタが適用され、マッチ候補リストが確定する
- マッチ候補リストが 1 件以上の場合、`activeIndex = 0`（先頭候補）を自動でアクティブにする
- マッチ候補リストが 0 件の場合、`activeIndex = -1`（未選択）のままにする

#### FR-12-2: Enter キーによる次候補への移動

- 検索欄フォーカス中に Enter キーが押されたとき（IME 確定中の Enter は除く）:
  - `activeIndex` を 1 加算する
  - `activeIndex` がマッチ候補リストの末尾を超えた場合は 0 に戻す（循環）
  - アクティブ候補が画面外にある場合は当該候補が見えるようにスクロールする（§4 NF-5 参照）

#### FR-12-3: 上下矢印キーによる候補移動

- 検索欄フォーカス中に ↓ キーが押されたとき: Enter キーと同じく次候補へ移動（循環あり）
- 検索欄フォーカス中に ↑ キーが押されたとき:
  - `activeIndex` を 1 減算する
  - `activeIndex` が -1 を下回った場合はリスト末尾（最後の候補）に戻る（循環）
  - アクティブ候補が画面外にある場合はスクロールして表示する

#### FR-12-4: IME 確定 Enter の誤動作防止

- `compositionstart` から `compositionend` の間に発火した `keydown` の Enter は FR-12-2 の処理を実行しない
- `compositionend` イベント後に発火した Enter のみ候補移動対象とする

---

## 4. 非機能要件

### NF-1: パフォーマンス

- クライアント側フィルタのみ使用し、検索ごとの追加 API コールは発生させない
- デバウンス 300ms により、高速タイピング時の過剰な再描画を防ぐ
- `KC.State.events` の最大件数は `KC.Config.QUERY_LIMIT = 500` 件。この規模においてクライアント側フィルタのメモリ処理は実用的（未検証: 大規模アプリでの計測は将来対応）

### NF-2: アクセシビリティ

- `<input>` に `aria-label="予定を検索"` を付与する
- クリアボタンに `aria-label="検索をクリア"` を付与する
- 検索欄にラッパー要素 `role` は付与しない（`<input type="search">` の暗黙ロールで十分）
- 結果 0 件メッセージ要素（`#kc-search-empty`）に `aria-live="polite"` を付与し、スクリーンリーダーへの通知を行う
- キーボードショートカットによる検索欄への自動フォーカス機能は実装しない（v2 確定）
- アクティブ候補の `.kc-event--match-active` 要素に `tabindex="-1"` を付与することで、スクリーンリーダー向けのフォーカス可能性を確保することを検討する（未確定）

### NF-3: 後方互換性

- `KC.SearchFilter` モジュールを新規追加するのみで、既存モジュールの動作を変更しない
- 検索クエリが空（初期状態）の場合は検索フィルタの効果がなく、既存の動作と完全に一致する

### NF-4: モバイル / 狭幅対応

- ヘッダー右側のアイテム数が増えるため、狭幅時のレイアウト崩れを防ぐ
- 推奨案: 400px 以下では検索入力欄を折り畳み（アイコンボタン化）し、タップで展開する（§11.1 参照）

### NF-5: スクロール・ジャンプ動作（v2 追加）

フォーカス遷移（FR-12）によってアクティブ候補が変わった際のスクロール挙動を定義する。

- アクティブ候補が現在の表示領域外にある場合は、当該候補が視野内に入るよう自動スクロールする
- スクロールには `element.scrollIntoView({ block: 'nearest', behavior: 'smooth' })` を利用することを推奨（未確認: 各ビューのスクロールコンテナ構造に依存するため実装時に調整）
- 月ビューでアクティブ候補が現在表示中の月と異なる月に属する場合の挙動は §11.8 参照

---

## 5. UI 仕様

### 5.1 ヘッダーバーレイアウト（ASCII モック）

```
+----------------------------------------------------------------------+
| [kc-head-left]                    [kc-head-right]                    |
|                                                                      |
| タイトル [今日] [<] [>] 2026年5月  [🔍 予定を検索 ×] [すべて ▾] [月 ▾] [⛶] [⚙]|
+----------------------------------------------------------------------+
```

- `[🔍 予定を検索 ×]` が検索欄（FR-2）
- `×` はクリアボタン。クエリ空時は非表示

### 5.2 検索欄の HTML 構造

```html
<div id="kc-search" class="kc-search" role="search">
  <span class="kc-search-icon" aria-hidden="true">🔍</span>
  <input
    type="search"
    id="kc-search-input"
    class="kc-search-input"
    placeholder="予定を検索"
    aria-label="予定を検索"
    autocomplete="off"
  />
  <button
    type="button"
    id="kc-search-clear"
    class="kc-search-clear"
    aria-label="検索をクリア"
    hidden
  >×</button>
</div>
```

### 5.3 検索欄のスタイル概要

| プロパティ | 値（案） |
|---|---|
| `.kc-search` | `display: flex; align-items: center; border: 1px solid #c1c5ca; border-radius: 10px; height: 38px; padding: 0 8px; background: #fff;` |
| `.kc-search-input` | `border: none; outline: none; font-size: 14px; width: 160px; background: transparent;` |
| `.kc-search-clear` | `border: none; background: transparent; cursor: pointer; font-size: 14px; color: #777;` |
| `.kc-search-icon` | `font-size: 14px; color: #777; margin-right: 4px;` |
| 狭幅時（max-width: 400px） | `.kc-search-input { width: 0; }` + アイコンクリックで幅を展開（§11.1） |

### 5.4 マッチ候補のハイライトスタイル（v2 追加）

```
カレンダー上の予定要素:

[通常表示（クエリ空）]
  .kc-event: 既存スタイルのみ。ハイライトなし

[マッチ候補（クエリ非空、activeIndex 以外）]
  .kc-event.kc-event--match
  → outline: 2px solid #2563eb;  /* 青細枠 */

[アクティブ候補（activeIndex が指す予定）]
  .kc-event.kc-event--match.kc-event--match-active
  → outline: 3px solid #1d4ed8;  /* 青太枠 */
  → box-shadow: 0 0 0 3px rgba(37,99,235,0.3);  /* 外周光彩 */

[非一致予定（クエリ非空、マッチしない）]
  .kc-event（クラス追加なし）
  → opacity: 0.25;  /* インラインスタイルで付与 */
```

ASCII モック（検索「会議」、アクティブ = 月次会議）:

```
+------------------+  +------------------+  +------------------+
|  月次会議        |  |  週次会議        |  |  プロジェクト報告 |
| ★アクティブ候補  |  | (通常マッチ)     |  | (非一致・半透明) |
| [太枠+外周光彩]  |  | [細枠]           |  | [opacity 0.25]   |
+------------------+  +------------------+  +------------------+
```

### 5.5 状態遷移（v2 更新）

```
[初期状態: クエリ空]
  入力欄: 空, プレースホルダー表示
  クリアボタン: hidden
  カレンダー: 通常表示（全予定）
  activeIndex: -1

  ↓ ユーザーが文字を入力 & 300ms 経過（または IME 確定）

[検索中: クエリ非空]
  入力欄: テキスト表示
  クリアボタン: 表示
  一致予定: .kc-event--match クラス付与（細枠ハイライト）
  非一致予定: opacity 0.25
  activeIndex: 0（先頭マッチ候補を自動アクティブ化）
  アクティブ候補: .kc-event--match-active クラス付与（太枠）

  ↓ Enter または ↓ キー
  activeIndex += 1（末尾超えで 0 に戻る）
  アクティブ候補が画面外 → スクロール

  ↓ ↑ キー
  activeIndex -= 1（-1 下回りで末尾インデックスに戻る）
  アクティブ候補が画面外 → スクロール

  ↓ クリアボタン or バックスペースで全削除

[初期状態に戻る]
  .kc-event--match / .kc-event--match-active クラスを全解除
  opacity スタイルをリセット
  activeIndex: -1

  ↓ 一致件数が 0 になった場合

[0件状態]
  カレンダーグリッド内に「「{query}」に一致する予定はありません」を表示
  activeIndex: -1
```

### 5.6 ビュー再描画時の検索フィルタ適用位置

```
R.refresh() が呼ばれる
  ↓
KC.EventFilter.apply(KC.State.events)   // 既存フィルタ（mine/others/all）
  ↓
KC.SearchFilter.apply(filteredEvents)  // 検索フィルタ（新規）
  ↓
描画対象イベント配列 → ビュー描画関数へ渡す
  ↓
KC.SearchFilter.applyHighlight()       // マッチ候補クラス付与・activeIndex 同期
```

---

## 6. データ構造

### 6.1 KC.SearchFilter モジュール（新規）

```javascript
KC.SearchFilter = {
  query: '',           // 現在の検索クエリ（空文字 = 無効）
  _timer: null,        // デバウンス用タイマーID
  _composing: false,   // IME 入力中フラグ
  activeIndex: -1,     // アクティブ候補の index（-1 = 未選択）
  _matchList: [],      // 現在のマッチ候補 DOM 要素リスト（表示順）

  /** 検索クエリを設定し、再描画をトリガーする */
  setQuery: function(q) { ... },

  /** events 配列に検索フィルタを適用し、結果配列を返す */
  apply: function(events) { ... },

  /** クエリが空または一致判定 */
  _matches: function(evt) { ... },

  /** 描画後にマッチ候補 DOM 要素へクラスを付与し、activeIndex を適用する */
  applyHighlight: function() { ... },

  /**
   * activeIndex に対応する候補をアクティブにし、必要に応じてスクロールする
   * @param {number} newIndex - 新しい activeIndex
   */
  _activateAt: function(newIndex) { ... },

  /** Enter または ↓ キー押下時: 次候補に移動 */
  focusNext: function() { ... },

  /** ↑ キー押下時: 前候補に移動 */
  focusPrev: function() { ... },

  /** UI 初期化（_buildDOM で kc-head-right に追加） */
  buildDOM: function(headRight) { ... },

  /** クリアボタンの表示/非表示を同期する */
  _syncClearBtn: function() { ... }
};
```

### 6.2 apply() の動作仕様

```
KC.SearchFilter.apply(events):
  if this.query === '':
    return events  // フィルタ無効: 全件返す

  tokens = this.query.trim().toLowerCase()
             .split(/[\s　]+/)   // 半角スペース・全角スペースで分割
             .filter(t => t !== '')

  return events.filter(evt =>
    tokens.every(token => (evt.title || '').toLowerCase().includes(token))
  )
```

### 6.3 applyHighlight() の動作仕様（v2 追加）

```
KC.SearchFilter.applyHighlight():
  // 既存クラスをリセット
  querySelectorAll('.kc-event--match').forEach(el => el.classList.remove('kc-event--match', 'kc-event--match-active'))
  // 非一致予定の opacity もリセット
  querySelectorAll('.kc-event').forEach(el => el.style.opacity = '')

  if this.query === '':
    this._matchList = []
    this.activeIndex = -1
    return

  // 全 .kc-event 要素をスキャンし、マッチ/非マッチを分類
  allEventEls = querySelectorAll('.kc-event')  // カレンダー表示中の全予定 DOM
  this._matchList = []

  for each el in allEventEls:
    eventTitle = el.dataset.title  // または el に紐付く KcEvent.title の参照方法に依存（実装時要確認）
    if _matches({ title: eventTitle }):
      el.classList.add('kc-event--match')
      this._matchList.push(el)
    else:
      el.style.opacity = '0.25'

  // activeIndex の調整
  if this._matchList.length === 0:
    this.activeIndex = -1
  else if this.activeIndex === -1 or this.activeIndex >= this._matchList.length:
    this.activeIndex = 0

  // アクティブ候補にクラス付与
  if this.activeIndex >= 0:
    this._matchList[this.activeIndex].classList.add('kc-event--match-active')
```

### 6.4 マッチ候補リストの順序

`_matchList` の要素順は **DOM 上の表示順**（`querySelectorAll` の返却順）とする。これはカレンダー上での視覚的な表示順に近い。

詳細な順序定義は §11.9 を参照。

### 6.5 sessionStorage への保存（Phase 1: 保存しない）

Phase 1 では検索クエリを sessionStorage に保存しない。ページリロードや kintone ビュー再表示時に空にリセットされる。

将来 Phase 2 で sessionStorage 保存が必要になった場合のキー案:

```
sessionStorage キー: 'kc-search-query'
値: 検索クエリ文字列（最大 200 文字を推奨）
```

### 6.6 既存 KC.State への追加

`KC.State` に `searchQuery` プロパティを追加することは**しない**。`KC.SearchFilter.query` で管理する。

---

## 7. 既存実装への変更点

> 注意: 本セクションはコードの静的調査に基づく推測を含む。「未確認」と記載した箇所は実機確認を要する。

### 7.1 desktop.js の変更（見込み）

| 箇所 | 変更内容 | 確認状況 |
|---|---|---|
| `KC.Boot._buildDOM()` (`desktop.js:4824`付近) | `headRight` の先頭要素として `kc-search` を追加する処理を追加（`insertBefore`） | 調査済 |
| `KC.Boot.init()` (`desktop.js:5179`付近) | `KC.SearchFilter.buildDOM(headRight)` の呼び出しを追加（`KC.FilterDropdown.init()` の直前） | 調査済 |
| `KC.MonthView.render()` 等、各ビュー描画関数 | `KC.EventFilter.apply()` の戻り値をさらに `KC.SearchFilter.apply()` に渡すよう変更 | 未確認 |
| `R.refresh()` または各ビュー描画の呼び出し元 | 描画完了後に `KC.SearchFilter.applyHighlight()` を呼ぶよう変更（§5.6 参照） | 未確認 |
| 検索欄 `keydown` イベントハンドラ（新規） | Enter / ↓ キーで `KC.SearchFilter.focusNext()`、↑ キーで `KC.SearchFilter.focusPrev()` を呼ぶ。IME 確定中は無視（FR-12-4） | 未確認 |
| `app.record.index.show` イベントハンドラ | ビュー再表示時に `KC.SearchFilter.setQuery('')` を呼ぶ処理を追加（FR-9） | 未確認 |
| ビュー切替処理 | ビュー切替時に `KC.SearchFilter.activeIndex = -1` をリセットする処理を追加（FR-7） | 未確認 |

### 7.2 desktop.css の変更（見込み）

| 追加クラス | 内容 | 確認状況 |
|---|---|---|
| `.kc-search` | 検索欄ラッパーのスタイル（flex, border, border-radius, height） | 新規追加 |
| `.kc-search-input` | テキスト入力欄のスタイル（border: none, width, font-size） | 新規追加 |
| `.kc-search-clear` | クリアボタンのスタイル | 新規追加 |
| `.kc-search-icon` | 検索アイコンのスタイル | 新規追加 |
| `.kc-search--active` | クエリ非空時にボーダー色変更等の追加スタイル（任意） | 新規追加（任意） |
| `.kc-event--match` | マッチ候補の細枠ハイライト（`outline: 2px solid #2563eb`） | 新規追加 |
| `.kc-event--match-active` | アクティブ候補の太枠ハイライト（`outline: 3px solid #1d4ed8; box-shadow: ...`） | 新規追加 |
| `@media (max-width: 400px)` | 狭幅時の折り畳みスタイル（§11.1 確定後に追加） | 未確認 |

### 7.3 config.html / config.js の変更

Phase 1 では検索対象フィールドのプラグイン設定は不要のため、`config.html` / `config.js` への変更はなし。

### 7.4 プラグイン設定 JSON スキーマへの変更

Phase 1 では変更なし。Phase 2 で検索対象フィールドを設定可能にする場合は `fieldMapping.searchTargets: string[]` を追加予定。

---

## 8. 影響範囲 / 非干渉確認

### 8.1 権限色（v6.1）との干渉

`REQ_edit-permission-extension.md` の v5/v6 で実装された権限色（`bgColor` / `textColor` のインラインスタイル）と検索フィルタの相互作用を確認する。

| 状況 | 期待動作 |
|---|---|
| 検索クエリ非空、かつ一致予定に権限色あり | 権限色（`bgColor` / `textColor`）は維持したまま、`opacity` は変更しない |
| 検索クエリ非空、かつ非一致予定に権限色あり | 権限色を維持したまま `opacity: 0.25` を適用する |
| マッチ候補（権限色あり） | 権限色を維持したまま `.kc-event--match` / `.kc-event--match-active` の `outline` を付与する |

- `opacity` は要素全体に作用するため、`bgColor` / `textColor` のインラインスタイルとは独立して適用できる
- `outline` も `bgColor` / `textColor` と干渉しない（未確認 — 実機での重ね合わせ動作を要確認）

### 8.2 FilterDropdown（mine/others/all）との干渉

- 検索フィルタと eventFilter は独立した 2 層のフィルタとして設計（FR-10）
- `KC.EventFilter.apply()` の後に `KC.SearchFilter.apply()` を適用するため、eventFilter で除外された予定は検索対象から除外される（仕様どおり）
- eventFilter 変更時は `R.refresh()` が呼ばれ、検索フィルタも自動的に再適用される（未確認 — refresh フロー確認要）

### 8.3 ビュー個別設定（REQ_view-config-popup）との干渉

- ビュー切替は `R.refresh()` で再描画されるため、検索クエリが `KC.SearchFilter.query` に保持されていれば再描画時にも自動適用される
- ビュー切替時は `activeIndex` をリセットする（FR-7）
- 干渉なし（予定）

### 8.4 全画面表示機能との干渉

- 全画面時もヘッダーは表示されるため、検索欄は全画面時でも利用可能
- `kc-expanded` クラスの CSS が検索欄の表示に干渉しないことを確認すること（未確認）

---

## 9. 受入基準

### AC-1: 検索欄がヘッダーバーに表示される

- **Given**: カレンダービューを開く
- **When**: ヘッダーバーを確認する
- **Then**: `#kc-search` が `.kc-head-right` 内の最左位置（FilterDropdown より左）に表示される

### AC-2: 入力に応じてカレンダーが絞り込まれる

- **Given**: 複数の予定が表示されているカレンダー（タイトル「月次会議」と「週次報告」を含む）
- **When**: 検索欄に「会議」と入力する（入力から 300ms 経過後）
- **Then**: 「月次会議」は `.kc-event--match` クラスが付与されハイライトされる
- **And**: 「週次報告」は半透明（opacity 0.25）になる

### AC-3: クリアボタンで検索がリセットされる

- **Given**: 検索欄に「会議」と入力し、絞り込みが適用されている
- **When**: クリアボタン（×）をクリックする
- **Then**: 入力欄が空になる
- **And**: 全予定が通常表示（opacity リセット、ハイライトクラス解除）される
- **And**: クリアボタンが非表示になる

### AC-4: クリアボタンはクエリが空のとき非表示

- **Given**: カレンダービューを開く（検索クエリ空）
- **When**: ヘッダーバーを確認する
- **Then**: クリアボタン（`#kc-search-clear`）が `hidden` 状態で非表示になっている

### AC-5: スペース区切り AND 検索が機能する

- **Given**: タイトル「月次会議 全員参加」と「月次報告」の予定が存在する
- **When**: 検索欄に「会議 全員」と入力する
- **Then**: 「月次会議 全員参加」は `.kc-event--match` クラスが付与される
- **And**: 「月次報告」は半透明になる

### AC-6: 大文字小文字を区別しない

- **Given**: タイトル「Team Meeting」の予定が存在する
- **When**: 検索欄に「meeting」と入力する
- **Then**: 「Team Meeting」の予定が `.kc-event--match` クラスを持つ

### AC-7: ビュー切替で検索状態が維持される

- **Given**: 月ビューで「会議」と検索し、絞り込みが適用されている
- **When**: ビュー切替ドロップダウンで週ビューに切り替える
- **Then**: 週ビューでも「会議」の絞り込みが継続している
- **And**: 検索欄に「会議」が入力されたままである
- **And**: 週ビューのマッチ候補に対して `activeIndex = 0` で先頭候補がアクティブになる

### AC-8: 0 件時にメッセージが表示される

- **Given**: カレンダーに予定が存在する
- **When**: 検索欄に「zzz存在しないキーワード」と入力する
- **Then**: `#kc-search-empty` に「「zzz存在しないキーワード」に一致する予定はありません」が表示される

### AC-9: IME 入力中は検索が発火しない

- **Given**: 日本語 IME が有効な環境でカレンダーを表示している
- **When**: 検索欄で「かいぎ」と入力中（未確定状態）
- **Then**: `compositionend` が発火するまでカレンダーは再描画されない

### AC-10: FilterDropdown との独立動作

- **Given**: eventFilter が「自分のみ（mine）」に設定されている
- **When**: 検索欄に「会議」と入力する
- **Then**: 「自分のみ」フィルタを通過した予定の中から「会議」に一致するものが `.kc-event--match` でハイライトされる
- **And**: 「自分のみ」フィルタで非表示だった予定は引き続き非表示のままである

### AC-11: 検索欄がページ再表示時にリセットされる

- **Given**: 検索欄に「会議」と入力し、絞り込みが適用されている
- **When**: kintone の一覧ビューを離れて再表示する（`app.record.index.show` が再発火）
- **Then**: 検索欄が空になる
- **And**: 全予定が通常表示される

### AC-12: Enter キーで次のマッチ候補にフォーカス移動する（v2 追加）

- **Given**: 検索欄に「会議」と入力し、マッチ候補が複数存在する
- **When**: 検索欄にフォーカスがある状態で Enter キーを押す（IME 未使用）
- **Then**: `activeIndex` が 1 増加し、次のマッチ候補に `.kc-event--match-active` クラスが付与される
- **And**: 前のアクティブ候補から `.kc-event--match-active` クラスが外れる

### AC-13: Enter キーの循環動作（v2 追加）

- **Given**: 検索欄に「会議」と入力し、マッチ候補が 3 件（index 0, 1, 2）存在し、現在 index 2 がアクティブ
- **When**: 検索欄にフォーカスがある状態で Enter キーを押す
- **Then**: `activeIndex` が 0 に戻り、先頭のマッチ候補がアクティブになる（循環）

### AC-14: 上下矢印キーで候補を移動できる（v2 追加）

- **Given**: 検索欄に「会議」と入力し、マッチ候補が複数存在し、index 1 がアクティブ
- **When**: ↑ キーを押す
- **Then**: `activeIndex` が 0 になり、前の候補がアクティブになる
- **When**: さらに ↑ キーを押す（index 0 の状態で）
- **Then**: `activeIndex` が末尾インデックスに戻る（循環）

### AC-15: アクティブ候補の視覚区別（v2 追加）

- **Given**: 検索欄に「会議」と入力し、マッチ候補が複数存在する
- **When**: カレンダー画面を確認する
- **Then**: アクティブ候補（`.kc-event--match-active`）は他のマッチ候補（`.kc-event--match`）より太い枠で表示される

### AC-16: IME 確定 Enter はフォーカス遷移しない（v2 追加）

- **Given**: 検索欄にフォーカスがあり、マッチ候補が存在する
- **When**: IME で「かいぎ」を入力し、Enter キーで変換確定する
- **Then**: `activeIndex` は変化しない（フォーカス遷移は発生しない）

---

## 10. 検証項目

### T-1: 正常系 — 1 語検索

| # | 操作 | 期待結果 |
|---|---|---|
| T-1-1 | タイトル「会議」「報告」の 2 予定がある状態で「会議」を入力 | 「会議」に `.kc-event--match` 付与, 「報告」半透明 |
| T-1-2 | 入力後すぐ（300ms 未満）に別の文字を追加 | 前の入力ではカレンダー再描画が発生しない（デバウンス確認） |
| T-1-3 | 入力欄を全削除してクエリを空にする | 全予定が通常表示される、ハイライトクラス全解除 |

### T-2: 正常系 — AND 検索

| # | 操作 | 期待結果 |
|---|---|---|
| T-2-1 | 「月次 会議」（半角スペース）で検索 | 両方のトークンを含むタイトルのみ `.kc-event--match` 付与 |
| T-2-2 | 「月次　会議」（全角スペース）で検索 | T-2-1 と同じ動作 |
| T-2-3 | スペースのみ入力（「   "」） | 空クエリと同等: 全予定通常表示 |

### T-3: 正常系 — クリアボタン

| # | 操作 | 期待結果 |
|---|---|---|
| T-3-1 | クエリ非空の状態でクリアボタンをクリック | 入力欄空、全予定通常表示、クリアボタン非表示、ハイライトクラス全解除 |
| T-3-2 | クリア後に検索アイコンを確認 | 検索アイコンは常に表示されている |

### T-4: 正常系 — ビュー間での検索維持

| # | 操作 | 期待結果 |
|---|---|---|
| T-4-1 | 月ビューで「会議」検索後、週ビューに切替 | 週ビューでも「会議」絞り込みが適用される、activeIndex=0 でリセット |
| T-4-2 | 週ビューで「会議」検索後、日ビューに切替 | 日ビューでも「会議」絞り込みが適用される |

### T-5: 正常系 — 0 件メッセージ

| # | 操作 | 期待結果 |
|---|---|---|
| T-5-1 | 存在しないキーワードを入力 | 0 件メッセージが表示される |
| T-5-2 | 0 件状態でクリアボタンをクリック | メッセージが消え、全予定通常表示 |
| T-5-3 | 0 件状態で一部文字を削除して 1 件以上が一致 | メッセージが消える |

### T-6: 正常系 — FilterDropdown との組み合わせ

| # | 操作 | 期待結果 |
|---|---|---|
| T-6-1 | eventFilter=「自分のみ」 + 検索「会議」 | 自分の予定かつ「会議」を含む予定のみ `.kc-event--match` 付与 |
| T-6-2 | 検索「会議」の状態で eventFilter を「すべて」に変更 | 検索クエリは「会議」のまま、全ユーザーの「会議」予定が対象になる |

### T-7: 境界値・異常系

| # | 操作 | 期待結果 |
|---|---|---|
| T-7-1 | 検索クエリに特殊文字（`<`, `>`, `"`, `&`）を入力 | XSS が発生しない（innerHTML を使わない実装であること） |
| T-7-2 | 200 文字を超える長い文字列を入力 | エラーが発生せず、部分一致検索が正常に動作する |
| T-7-3 | 予定が 0 件のカレンダーで検索を入力 | 0 件メッセージが表示される（エラーなし） |
| T-7-4 | 日本語を IME で入力し、変換確定前の状態 | カレンダーは再描画されない（compositionend 待機） |
| T-7-5 | 全画面表示中に検索を入力 | 全画面モード中でも検索が機能する |

### T-8: アクセシビリティ確認

| # | 確認内容 |
|---|---|
| T-8-1 | 検索入力欄に Tab キーでフォーカスできる |
| T-8-2 | クリアボタンに Tab キーでフォーカスできる |
| T-8-3 | `aria-live="polite"` の 0 件メッセージがスクリーンリーダーで読み上げられる（実機未確認） |

### T-9: フォーカス遷移 — Enter キー（v2 追加）

| # | 操作 | 期待結果 |
|---|---|---|
| T-9-1 | 検索「会議」（マッチ候補 3 件）、Enter キーを 1 回押す | activeIndex が 0 → 1 に移動。index 1 の予定が `.kc-event--match-active` |
| T-9-2 | T-9-1 からさらに Enter を 2 回押す（計 3 回）| activeIndex が 1 → 2 → 0 に循環 |
| T-9-3 | マッチ候補が 1 件のみで Enter を押す | activeIndex は 0 のまま（循環しても同一候補） |
| T-9-4 | マッチ候補が 0 件の状態で Enter を押す | activeIndex は -1 のまま。エラーが発生しない |

### T-10: フォーカス遷移 — 上下矢印キー（v2 追加）

| # | 操作 | 期待結果 |
|---|---|---|
| T-10-1 | 検索「会議」（マッチ候補 3 件）、↓ キーを押す | activeIndex が 0 → 1 に移動 |
| T-10-2 | T-10-1 からさらに ↑ キーを押す | activeIndex が 1 → 0 に戻る |
| T-10-3 | activeIndex = 0 の状態で ↑ キーを押す | activeIndex が 2（末尾）に循環 |
| T-10-4 | activeIndex = 末尾 の状態で ↓ キーを押す | activeIndex が 0 に循環 |

### T-11: フォーカス遷移 — スクロール（v2 追加）

| # | 操作 | 期待結果 |
|---|---|---|
| T-11-1 | マッチ候補が画面外にある状態で Enter を押して当該候補をアクティブにする | 当該候補が画面内に入るよう自動スクロールされる |
| T-11-2 | 週ビューで上部の時間帯にある候補から下部の時間帯にある候補へ Enter で移動 | 下部候補が表示される位置までスクロールされる |

### T-12: フォーカス遷移 — IME 誤動作防止（v2 追加）

| # | 操作 | 期待結果 |
|---|---|---|
| T-12-1 | 検索欄で IME 入力中（compositionend 前）に Enter キーを押す | activeIndex は変化しない |
| T-12-2 | IME で変換確定（Enter 押下）後、再度 Enter を押す | activeIndex が 1 増加する（確定後の Enter は有効） |

---

## 11. 未確定事項・リスク・前提

### 11.1 Q1: 狭幅時（モバイル）レイアウト

ヘッダー右側に要素が増えるため、狭幅時にヘッダーが折り返しまたは溢れる可能性がある。

**選択肢**:
- **案 A（推奨）**: 400px 以下で検索入力欄を幅 0 に折り畳み、虫眼鏡アイコンをタップで展開する
- 案 B: 狭幅時に検索欄をヘッダー下の別行に移動する（2 行ヘッダー）
- 案 C: 狭幅時はヘッダー右側の要素を省略メニューに格納する

**推奨**: 案 A。既存の `flex-wrap: nowrap` を維持しつつ、最小限の CSS 変更で対応できる。

**ユーザー確認事項**: 案 A でよいか。主要利用デバイスがデスクトップのみであれば狭幅対応は Phase 2 送りでもよい。

### 11.2 Q2: 非一致予定の表示方法（v2 で方針確定）

ユーザー追加要望（v2）にて以下を確認:
- **案 α（採用）**: 半透明（`opacity: 0.25`）。マッチ候補が「枠強調 + 半透明背景」の対比で目立ち、かつ全体感も保てる
- 案 β: 通常表示（非マッチも普通に表示、マッチのみハイライト）
- 案 γ: 非表示

**推奨**: 案 α（半透明）。ユーザー追加要望でも推奨と明記されており、v2 では案 α を正式採用とする。ただし実装後の見栄えを確認し、必要であれば opacity 値を調整する。

### 11.3 Q3: 再描画方式

**選択肢**:
- **案 A（推奨）**: `R.refresh()` を呼ぶ（既存の全再描画）。確実だが重い
- 案 B: 既存 DOM の各イベント要素の `opacity` と class のみを変更する（差分更新）。軽いが実装が複雑

**推奨**: 案 A。既存の `R.refresh()` パスに検索フィルタを組み込む最小変更で済む。予定 500 件以下では体感的に問題ないと見込む（未検証）。

**ユーザー確認事項**: 初期実装は案 A で進め、実際のパフォーマンス問題が発生した場合に案 B に移行する方針でよいか。

### 11.4 Q4: 検索アイコンの素材

**選択肢**:
- **案 A（推奨）**: Unicode 絵文字 `🔍`（実装が簡単）
- 案 B: SVG アイコン（既存の全画面ボタンと同様。統一感がある）

**推奨**: 案 A で実装し、デザインレビュー後に案 B に切り替えることも可能。

### 11.5 Q5: sessionStorage への保存

**選択肢**:
- **案 A（推奨 / Phase 1）**: 保存しない。ページリロードや再表示時にリセット
- 案 B: `sessionStorage['kc-search-query']` に保存。タブを閉じるまで保持

**推奨**: 案 A（保存しない）。同一アプリを複数タブで開く運用時の干渉を避ける。Phase 2 でユーザーニーズを確認してから案 B を検討する。

**ユーザー確認事項**: リロード後も検索状態を維持したい場合は案 B を選択する。

### ~~11.6 Q6: キーボードショートカット~~ （v2 で削除・確定）

`/` キーによる検索欄フォーカスショートカットは**実装しない**（v2 にてユーザー判断で確定）。検索欄はユーザーがクリックしてフォーカスを当てる通常の入力欄として扱う。

### 11.7 Q7: kintone REST API クエリ投影（Phase 2 検討事項）

Phase 1 はクライアント側フィルタのみ。QUERY_LIMIT（500 件）を超える大規模アプリでは取得漏れが発生する。Phase 2 では以下の選択肢を検討する。

- 案 A: `kintone.api('/k/v1/records', 'GET', { query: 'title like "query"' })` で再フェッチ（パフォーマンス良・件数制限なし）
- 案 B: カーソル API でページング取得済みの全件に対してクライアント側フィルタ

Phase 1 では検討不要。500 件超のアプリで問題が発生した際に Phase 2 として起票する。

### 11.8 Q8: フォーカス候補が月跨ぎの場合の挙動（v2 新規追加）

月ビューでアクティブ候補（Enter/上下キーで移動した先）が現在表示中の月と異なる月に属する場合、月の切替を行うかどうかが未確定。

**選択肢**:
- **案 A（推奨）**: 切り替えない。現在表示中の月内のマッチ候補のみで巡回する。月を跨ぐマッチ候補はスキップする（または表示月内で末尾になったら循環）
- 案 B: 月を跨いで移動する。`activeIndex` が別月の候補を指した場合に月切替を発火させる

**推奨**: 案 A。月切替を伴うフォーカス遷移はユーザーの意図しないビュー変化を引き起こす可能性がある。現在表示月内で完結する方が安全。

**ユーザー確認事項**: 案 A でよいか。月跨ぎ検索が必要なユースケースがある場合は案 B を選択する。

### 11.9 Q9: マッチ候補リストの順序定義（v2 新規追加）

`_matchList`（フォーカス遷移の巡回対象）の要素順序が未確定。

**選択肢**:
- **案 A（推奨）**: DOM 上の表示順（`querySelectorAll('.kc-event--match')` の返却順）。実装が最もシンプル
- 案 B: 予定の開始時刻順（`KcEvent.start` でソート）。時系列的に自然
- 案 C: カレンダー上の視覚位置順（左→右、上→下）。ユーザーの視線移動と一致するが、各ビューで DOM 構造が異なるため実装が複雑

**推奨**: 案 A。月ビュー・週ビュー・日ビューで `querySelectorAll` の返却順が概ね時系列順に一致することが多い（未確認 — DOM 構造依存）。実機確認後に案 B に変更することも可能。

**ユーザー確認事項**: 案 A で進めるか。時系列順（案 B）が強く求められる場合は案 B を選択する。

### 11.10 前提条件

- `KC.Config.FIELD.title` が有効なフィールドコードにマッピングされていること（プラグイン設定済み）
- カレンダーが `desktop.js` の `KC.Boot.init()` から初期化されていること
- `KC.State.events` が `KcEvent` 配列として管理されていること（`desktop.js:602` `_recordToEvent` で変換済み）
- `.kc-event` 要素に予定タイトルを参照できる属性（`data-title` 等）が付与されているか、DOM から `KcEvent` オブジェクトを逆引きできる仕組みがあること（`applyHighlight()` の実装に必要 — 未確認）

### 11.11 リスク

| リスク | 対策 |
|---|---|
| `R.refresh()` の連続呼び出しによる描画ちらつき | デバウンス 300ms で緩和。問題が残れば差分更新（§11.3 案 B）に移行 |
| `opacity: 0.25` が権限色（bgColor/textColor）の視認性に影響する | 実機確認。視認性問題があれば `opacity: 0.35` 等に調整 |
| ヘッダー右側が狭幅時に溢れる | 案 A の折り畳みで対応（§11.1）。未実装の場合は `overflow: hidden` でクリップ |
| `querySelectorAll('.kc-event')` のスキャンで DOM 構造に依存する | 実装時に `.kc-event` クラスの付与状況を確認。未付与の場合はクラス追加が必要 |
| `_matchList` の DOM 要素がビュー再描画後に無効（stale）になる | `R.refresh()` 後に `applyHighlight()` を呼んで `_matchList` を再構築する |
| IME 確定 Enter とフォーカス遷移 Enter の競合 | `compositionend` フラグ管理で対応（FR-12-4, T-12） |

---

*本要件定義書は 2026-05-20 時点の実装調査に基づき作成。v2 で確定した事項は §3/§11 に反映済み。残る未確定事項は §11.1/§11.3/§11.4/§11.5/§11.8/§11.9 に整理済み。実装前にユーザー確認を行うこと。*
